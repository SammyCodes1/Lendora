"use client";

/**
 * @deprecated Testnet toggle has been retired. The protocol is now exclusively on Arc Mainnet (Chain ID 5042).
 */
export function NetworkModeToggle(_props?: { className?: string; mobile?: boolean }) {
  return null;
}
